# Jira GitHub Practice

## Frontend

- Open `/index.html` for the home page.
- Navigate to `/card-list.html` to view the card list page.

The card list uses a reusable component in `/scripts/card.js` and sample data in `/scripts/card-list-page.js`.
